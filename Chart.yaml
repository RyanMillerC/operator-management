apiVersion: v2
name: operator-management
description: Manage operators via OLM with declarative versioning using manual upgrade mode with auto-approving InstallPlans based on Helm values
type: application
version: 1.0.0
